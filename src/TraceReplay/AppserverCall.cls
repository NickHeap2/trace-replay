USING Progress.Lang.*.

BLOCK-LEVEL ON ERROR UNDO, THROW.

CLASS TraceReplay.AppserverCall:
  DEFINE PUBLIC PROPERTY ProcedureName AS CHARACTER NO-UNDO
  GET.
  SET.
  DEFINE PUBLIC PROPERTY InternalProcedureName AS CHARACTER NO-UNDO
  GET.
  SET.
  DEFINE PUBLIC PROPERTY Parameters AS CHARACTER NO-UNDO
  GET.
  SET.
END CLASS.